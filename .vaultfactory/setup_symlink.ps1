$linkPath = ".vaultfactory"; $targetFolder = "C:\Repos\gd-obsidian-agentic-vault-architect"; $nestedLinkName = "architect"; $nestedLinkPath = Join-Path $linkPath $nestedLinkName; if ((Get-Item -LiteralPath $linkPath -Force).LinkType -eq 'SymbolicLink') { Remove-Item -LiteralPath $linkPath -Force } ; if (-Not (Test-Path $linkPath)) { New-Item -ItemType Directory -Path $linkPath | Out-Null } ; if (Test-Path $nestedLinkPath) { Remove-Item -LiteralPath $nestedLinkPath -Force } ; New-Item -ItemType SymbolicLink -Path $nestedLinkPath -Target $targetFolder | Out-Null ; Get-ChildItem -Path $linkPath -Force | Select-Object Mode,Name,Target